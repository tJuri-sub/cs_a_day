//contatenate boolean and string without getting an error

bool isString = true;

string name = "juri turiano";

Console.WriteLine(string.Concat(isString, " ", name));

Console.WriteLine(isString + " " + name);

